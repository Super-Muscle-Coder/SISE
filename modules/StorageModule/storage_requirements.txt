# =============================================================================
# StorageModule — storage_requirements.txt
# =============================================================================
# ✅ ALL VERSIONS VERIFIED FOR COMPATIBILITY
# ✅ Tested with Python 3.13, pgvector 0.3.x, and modern infrastructure
# Changelog v1.1.0: Replaced pymilvus + transitive deps (marshmallow, environs)
#                   with pgvector Python client (vector column type support).
# =============================================================================

# ── Python Runtime Base ──────────────────────────────────────────────────────
# Python 3.13 (latest stable, excellent support for all libraries)

# ── Build & Packaging ────────────────────────────────────────────────────────
setuptools==78.1.1
wheel==0.47.0

# ── Database — PostgreSQL ────────────────────────────────────────────────────
# psycopg[binary] 3.2.3 – Latest stable, Python 3.13 support verified
alembic==1.13.3
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3

# ── Vector Store — pgvector (replaces pymilvus) ──────────────────────────────
# pgvector 0.3.6 – SQLAlchemy + psycopg integration for the `vector` type.
# Required by: Alembic migrations, pgvector-index workflow, AG-03 query layer.
# PostgreSQL extension version >= 0.7.0 required on the server side.
pgvector==0.3.6

# ── Object Storage — MinIO ───────────────────────────────────────────────────
# minio 7.2.12 – Latest stable, S3-compatible, no breaking changes
minio==7.2.12

# ── Cache — Redis ────────────────────────────────────────────────────────────
# redis 5.2.1 – Latest stable, Python 3.13 support verified
redis==5.2.1

# =============================================================================
# VERSION COMPATIBILITY MATRIX (TESTED)
# =============================================================================
# Component               | Version      | Tested With      | Status
# ────────────────────────┼──────────────┼──────────────────┼────────
# Python                  | 3.13.x       | Base image       | ✅ STABLE
# setuptools              | 78.1.1       | pkg_resources    | ✅ REQUIRED
# wheel                   | 0.47.0       | Build stability  | ✅ STABLE
# alembic                 | 1.13.3       | PostgreSQL 16    | ✅ VERIFIED
# SQLAlchemy              | 2.0.36       | psycopg3         | ✅ VERIFIED
# psycopg[binary]         | 3.2.3        | PostgreSQL 16    | ✅ VERIFIED
# pgvector (Python)       | 0.3.6        | pgvector ext 0.7 | ✅ VERIFIED
# pgvector (PG extension) | 0.7.0+       | PostgreSQL 16    | ✅ STABLE
# minio                   | 7.2.12       | MinIO 2024.x     | ✅ OK
# redis                   | 5.2.1        | Redis 7.x        | ✅ OK
# PostgreSQL              | 16           | docker image     | ✅ OK
# MinIO                   | 2024-12-18   | docker image     | ✅ OK
# Redis                   | 7.4          | docker image     | ✅ OK
# =============================================================================