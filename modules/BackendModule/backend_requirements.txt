fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.0
python-dotenv==1.0.1

# Add in later workflow tasks:
# - sqlalchemy[asyncio], asyncpg          # auth/upload/indexing tasks
# - minio                                 # upload task
# - celery, redis client                  # indexing/idempotency tasks
# - python-jose, bcrypt                   # auth task
# - httpx                                 # AI/Storage integration tasks