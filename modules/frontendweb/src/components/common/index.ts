/**
 * @file index.ts
 * @layer components (Layer 3)
 * @description Export point for common components.
 *              SỬA: PageMeta đã dời sang services/usePageMeta.ts (không
 *              còn là component JSX — xem lý do trong file đó).
 * @owner AG-04
 */

export { Logo } from './Logo';